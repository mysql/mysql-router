# Example configuration file for router

[DEFAULT]
logdir = var/log
etcdir = etc
libdir = var/lib
rundir = var/run

[logger]
library = logger

[example]
library = example

[magic]
library = magic
message = Some kind of
