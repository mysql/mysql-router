[DEFAULT]
logdir = %(prefix)svar/log/%(program)s/
libdir = %(prefix)svar/lib/%(program)s/
rundir = %(prefix)svar/run/%(program)s/

[magic]
library = magic
message = It's some kind of magic

[example]
library = example

[bad_one]
library = bad_one

[bad_two]
library = bad_two
