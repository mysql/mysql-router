[DEFAULT]
logdir = %(prefix)svar/log/%(program)s
libdir = %(prefix)svar/lib/%(program)s
rundir = %(prefix)svar/run/%(program)s
etcdir = %(prefix)svar/run/%(program)s

[logger]
library = logger

# Two instance of the same plugin should throw an error, even if
# everything else is OK.
[example]
library = example

[example]
library = example
